#include <stdio.h>
void main()
{
    int n,f1=0,f2=1,f3=0,i=0;
    printf("Enter n: ");
    scanf("%d",&n);
    while(i<n)
    {   
        printf("%d\n",f3);
        f3 = f1+f2;
        f1 = f2;
        f2 = f3;
        i++;
    }
    
}
