#include<stdio.h>
int main()
{
 int parity=0,num;
 printf("enter a number :\n");
scanf("%d",&num);
while(num)
{
  if(num&1)
  parity=! parity;
  num=num>>1;
  num=num>>1;
 { 
  if(parity==0)
  printf("given parity is even");
  else
  printf("given parity is odd");
 }
}
return 0;
}
